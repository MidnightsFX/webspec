# spec helper is included

RSpec.describe 'Reservations' do
  # Before
  # Setup goes here

  context 'Book a Flight' do
    it 'Menu items should redirect accordingly' do
      # This should test all of the menu items under book a flight
      # these links are not 'external' but still need to work properly
    end
  end
  context 'Manage Reservations' do
    it 'Menu items should redirect accordingly' do
      # This should test all of the menu items under manage reservations
      # these links are not 'external' but still need to work properly
    end
  end
  context 'Make Other Reservations' do
    it 'These are all already tested in links' do
    end
  end
end # end describe
