# spec helper is included

RSpec.describe 'Travel Menu' do
  # Before
  # Setup goes here

  context 'Column 1' do
    it 'Menu items should redirect accordingly' do
      # This should test all of the menu items here
      # these links are not 'external' but still need to work properly
    end
  end
  context 'Column 2' do
    it 'Menu items should redirect accordingly' do
      # This should test all of the menu items here
      # these links are not 'external' but still need to work properly
    end
  end
  context 'Column 3' do
    it 'Menu items should redirect accordingly' do
      # This should test all of the menu items here
      # these links are not 'external' but still need to work properly
    end
  end
end # end describe
