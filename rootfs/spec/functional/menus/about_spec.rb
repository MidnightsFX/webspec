# spec helper is included

RSpec.describe 'About Menu' do
  # Before
  # Setup goes here

  context 'Corporate Information' do
    it 'Menu items should redirect accordingly' do
      # This should test all of the menu items under corporate info
      # these links are not 'external' but still need to work properly
    end
  end
  context 'Media and Partners' do
    it 'Menu items should redirect accordingly' do
      # This should test all of the menu items under media & partners
      # these links are not 'external' but still need to work properly
    end
  end
  context 'Business Services' do
    it 'Menu items should redirect accordingly' do
      # This should test all of the menu items under business services
      # these links are not 'external' but still need to work properly
    end
  end
end # end describe
